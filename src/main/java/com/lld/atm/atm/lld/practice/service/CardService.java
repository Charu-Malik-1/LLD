package com.lld.atm.atm.lld.practice.service;

import com.lld.atm.atm.lld.practice.models.Card;
import com.lld.atm.atm.lld.practice.models.Transaction;

public class CardService {

    public boolean validateCard(Card card){//Card card, double tranId){
        //call bank api

        return true;
    }

    public boolean validatePinAndAmount(int pin,int amount){
        return true;
    }
}
