package low_level_design1.my_vendingMachine.services;

public class AmountService {

    public boolean validateAmount(int amount){
        // check if amount is valid - no backend api is required as machine can validate
        return true;
    }
}